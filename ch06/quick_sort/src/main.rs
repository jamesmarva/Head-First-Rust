fn main() {
    let a = [1, 3, 4, 3, 2, 11];
    for item in &a {
        println!("{}", item);
    }

}
/// double point 
fn quick_sort(arr: &mut [i32]) {

}

fn qck_sort(arr: &mut [i32], start: i32, end: i32) {

}
fn quick_sort_core(arr: &mut [i32]) {
    let len = arr.len();
    let val = arr[0];
    let idx = 0;
    let mut l = 0;
    let mut r = len;
    while true {
    }
}
